import { AppointmentScheduleDto } from './appointment-schedule-dto';

describe('AppointmentScheduleDto', () => {
  it('should be defined', () => {
    expect(new AppointmentScheduleDto()).toBeDefined();
  });
});
